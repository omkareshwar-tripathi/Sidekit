# BRICKS.md

Session handoff log. **Read this first when starting a session.** Update the moment a brick is reviewed + tested + verified. See `CLAUDE.md` §2b for rules (and archiving to `BRICKS-ARCHIVE.md`).

Plan derived from `Sidekit-v1-spec.md` (the complete, decision-resolved spec). Manual test IDs (`M1`–`M8`) refer to `TESTING.md`. Architecture is **ports-and-adapters**: the pure core (Bricks 1–4) is built and unit-tested with fakes *before* any OS adapter exists.

**Branch model (since 2026-06-20):** all work is now on **`main`** (the public default branch / trunk). The old `feat/mac-app` Mac branch was merged into `main` and deleted, so the platform sections below both live on `main`. The sections organise the log by platform, not by branch. Other branches — `feat/correction-pipeline` (text-correction pipeline) and `chore/windows-folder` — still hold unmerged work to fold in later.

---

## Voice Agent Framework — on `feat/audiobook-agent`

_New direction (2026-06-22): now that the repo is open source, build reusable voice-driven **task agents** on top of Sidekit — each a `listen → decide → act → verify` pipeline (small Whisper → task-tuned LLM → tool/verify harness). Strategy: build ONE agent end-to-end as a real app, then extract the framework. First agent = an **audiobook player** (the "Hey Audible" example). Decomposition: **A** app shell + action schema → **B** harness + decide loop (prompt-steered baseline, then) → **D** fine-tune the task model; **C** always-on wake-word listening. Specs/plans in `docs/superpowers/`. Code lives in `agents/audiobook/` (new SwiftPM package; see `STRUCTURE.md`)._

### Next up (Voice Agent)

- [ ] **SCHEMA-CONSISTENCY — tighten the schema↔methods drift test (first brick of B).** `ActionSchemaTests` currently checks the 11 names/count/one param shape but does NOT verify each `ActionSchema` entry maps to a real `PlayerStore` method with matching params (the drift-catcher the spec's Testing § describes). The intentional `setSleepTimer` 1-action↔2-methods split is where future drift will hide. Add a reflection-free table test. Flagged by the final whole-branch review.
  - Skill: none (no Swift skill, per §6); TDD + verification apply.
- [ ] **AUDIOBOOK-MANUAL-UAT — user's manual Mac pass on the audiobook app.** Build & run `agents/audiobook` (`swift run AudiobookApp`); confirm: window + library render, Play plays real audio, all 11 controls move state correctly (incl. the goToChapter picker), sleep-timer "end of chapter" shows the right fire time, switchBook resets to 0:00 and loads the other title. Code/build/27 unit tests are green; only the visual+audio taste check is pending a human.
  - Skill: none (verification-before-completion).
- [ ] **Sub-project B — harness + decide loop (prompt-steered baseline).** Brainstorm → spec → plan before any brick. Wire transcript → function pick → execute → verify against the `ActionSchema`, using an off-the-shelf small model; this produces the labeled data + accuracy bar for the later fine-tune (D).
  - Skill: none (brainstorm first).

---

## Project Atlas — local re-onboarding dashboard (`.atlas/`)

_New workstream (2026-06-22): a **zero-dependency** local dashboard that re-onboards the maintainer after weeks away. Six layers — welcome-back ribbon, vision, progress kanban, decisions, learning (flashcards + spaced repetition), and AI operating context (with a cloud-portability audit). It **derives** from the repo's existing sources of truth (`vision/`, `BRICKS.md`, `docs/superpowers/specs/`, `.claude/`) rather than forking them. `sync.js` is built **adapter-shaped** (each source = a `detect()/read()` adapter) so the tool can later generalize to any repo without a rewrite (the "seam now, generalize later" decision). Code lives in `.atlas/`; runs on Node 18+. Domain `Skill:` lines are `none` (no JS skill in this repo, per §6); process discipline (verify-before-done) still applies._

### Next up (Atlas)

_All Atlas bricks complete. The board is fully functional; the Stop/SessionStart hooks are wired and additive. Remaining is the maintainer's visual taste-check in the browser (open http://127.0.0.1:7842) and the commit._

### Done (Atlas)

- [x] **ATLAS-WIRE — wired the two atlas hooks into `.claude/settings.json` (2026-06-22).**
  - **What:** With the maintainer's explicit authorization (the self-modification guard had blocked the agent-planned edit), appended a **third Stop** entry (`atlas-sync.sh` — refresh the board each turn) and a **second SessionStart** entry (`atlas-cloud-session.sh` — cloud env-refresh) to `settings.json`, keeping every existing entry.
  - **Files:** `.claude/settings.json`.
  - **Verified:** valid JSON; Stop = `check-bricks-updated, run-simplify-on-stop, atlas-sync` (original two intact + new third); SessionStart = `session-start-bricks, atlas-cloud-session` (original intact + new second); UserPromptSubmit untouched (§F.4.5 additive check passes).

- [x] **ATLAS-9 — cloud bootstrap scripts (2026-06-22).** *(SessionStart wiring → ATLAS-WIRE.)*
  - **What:** The pieces that make the atlas (and the project's AI config) behave in the remote cloud container. `.claude/scripts/cloud-setup.sh` — idempotent one-time container setup (`command -v` guards, ensures Node, seeds the board), with **GitNexus deliberately omitted** behind an explanatory comment so no future session tries to install the machine-bound binary. `.claude/hooks/atlas-cloud-session.sh` — a `CLAUDE_CODE_REMOTE`-guarded SessionStart helper that runs `sync.js --env-only` to fire the preserve-don't-delete merge; a no-op on the local machine. STRUCTURE.md updated (§7): `.claude/scripts/` noted in the `.claude/` row, and `.mcp.json` + `.claude/global-memory.md` documented as atlas portability artifacts.
  - **Files:** `.claude/scripts/cloud-setup.sh` (new), `.claude/hooks/atlas-cloud-session.sh` (new), `STRUCTURE.md`.
  - **Verified:** both scripts `bash -n` clean + executable. `atlas-cloud-session.sh` with `CLAUDE_CODE_REMOTE` unset → no-op exit 0; set → runs the env-only refresh (exit 0, environment.json stays valid `committed-only`). GitNexus omission comment present. **Pending:** the additive SessionStart wiring in `settings.json` (→ ATLAS-WIRE, needs maintainer OK).

- [x] **ATLAS-7 — `atlas-sync.sh` Stop-refresh hook (2026-06-22).** *(Stop wiring → ATLAS-WIRE.)*
  - **What:** A Stop hook that re-runs `node .atlas/sync.js` at the end of each turn so the board never drifts from real git/BRICKS state. Pure refresh: **never blocks the stop** (always exits 0), silent, with a loop guard and `.git`/node/`.atlas` existence guards. Mirrors `check-bricks-updated.sh` house-style (`set -uo pipefail`, single-purpose, bash-3.2-safe).
  - **Files:** `.claude/hooks/atlas-sync.sh` (new, executable).
  - **Verified:** `bash -n` clean; simulated Stop input → exit 0 + board refreshed (`git.json` regenerated); `stop_hook_active:true` loop-guard → fast no-op exit 0. **Pending:** the additive third Stop entry in `settings.json` (→ ATLAS-WIRE, needs maintainer OK).

- [x] **ATLAS-8 — promotion scaffolder: `sync.js --promote=<id>` + `POST /api/promote/:id` (2026-06-22).**
  - **What:** Turns a ⚠️ machine-local item into a committed artifact that actually applies in the cloud (plan §F.2). Three promotions, run on the user's machine only: **global memory** → writes `.claude/global-memory.md` (atlas-owned, provenance header) + a sentinel-fenced `@.claude/global-memory.md` import appended to CLAUDE.md (human §1–§7 prose never rewritten); **global skill** → copies `~/.claude/skills/<name>/` into `.claude/skills/` (existing → skip, committed wins); **global MCP** → converts `~/.claude.json` servers into project-scope `.mcp.json` with secrets replaced by `${ENV}` placeholders and the required vars listed in a top comment. Layer-6 gains a **Promote** button per promotable item (disabled with a tooltip in a committed-only/cloud snapshot, since the cloud can't see `~/.claude`).
  - **Files:** `.atlas/sync.js` (promote functions + `--promote=<id>` dispatch + sentinel/backup helpers), `.atlas/server.js` (`POST /api/promote/:id`, cloud-guarded), `.atlas/app.js` (`doPromote` + Promote buttons in `envRow`), `.atlas/styles.css` (`.btn.promote`, disabled state).
  - **Verified (sandbox repo + fake `$HOME`, so the real CLAUDE.md was untouched):** memory → global-memory.md + correct sentinel block in CLAUDE.md; skill → copied; MCP → `.mcp.json` with `API_KEY: "${CTX7_API_KEY}"` (secret redacted) + required-env comment. **Idempotent** (re-promote memory → still exactly 1 sentinel block). **Safety:** skill re-promote → "committed wins, skipped"; hand-edited `.mcp.json` (no `_atlas` marker) → **aborts, file not clobbered**; `--promote` under `CLAUDE_CODE_REMOTE` → "runs on your machine" (exit 1); server route in cloud → `{ok:false, "Run locally…"}`. Render still completes with the new buttons.
  - **Notes:** JSON can't hold comment-sentinels, so `.mcp.json` uses an `_atlas: true` marker as its "is-atlas-authored" sentinel; absence → treat as hand-edited and abort. `.bak` is written only when *overwriting* an existing atlas-authored file (first creation has nothing to back up). `markPromoted` (ATLAS-3b) flips these sources to ✅ on the next env sync once the artifact exists.

- [x] **ATLAS-6 — flashcards + SM-2 spaced-repetition review + persistence (2026-06-22).**
  - **What:** The learning aid — learn the concepts you hit while building. A pure, dependency-free **SM-2 scheduler** (`sm2.js`) drives in-dashboard review: pick "Review now", flip each due card, self-grade **Again/Hard/Good/Easy**, and the per-card state (`ease`, `interval`, `reps`, `lapses`, `due`) updates and persists. Also: an **Add card** form and a card list with new/learning/strong mastery badges. Seeded `learning.json` with 5 cards drawn from *this* build (ports-and-adapters, why environment.json is committed, SM-2 itself, the adapter seam, preserve-don't-delete) so the board teaches the project as you re-onboard.
  - **Files:** `.atlas/sm2.js` (new, pure scheduler), `.atlas/data/learning.json` (new, **committed** seed — review state travels), `.atlas/server.js` (`POST /api/review`, `POST /api/card`, learning read/write), `.atlas/app.js` (review panel + tiles + add form), `.atlas/styles.css` (buttons, review card, grade row, badges).
  - **Verified:** **SM-2 unit tests** (assert-based) all pass — intervals 1→6→×ease, easy raises ease, Again resets reps + counts a lapse + lowers ease, ease floored at 1.3, bad grade throws, input not mutated. Server: grade `c-sm2` Good → `reps 0→1, interval→1, due→2026-06-23`, **persisted to learning.json on disk (survives reload)**; invalid grade → 400; unknown card → 404; add card → count 5→6. DOM-shim render completes with no throw (learning body = 5 sections). Restored the 5-card seed after testing.
  - **Notes:** `learning.json` is committed on purpose (review state is part of the board that travels). SM-2 lives in its own module so it's unit-testable without HTTP. "Survives reload" is verified server-side (disk write); the click-through review flow is the maintainer's visual check in the browser.

- [x] **ATLAS-5 — edit-in-place → pending.json proposals (2026-06-22).**
  - **What:** Makes the board a real working tool without ever corrupting the canonical docs. The Vision north star, pitch, and open questions, plus the Decisions build-log notes, are now `contenteditable`; on blur (if changed) the edit POSTs to `/api/pending` and the server appends a `{layer, field, original, text, at}` proposal to `.atlas/data/pending.json`. A toast confirms "N proposals pending — Claude folds these into the docs." `vision/`, the specs, and `BRICKS.md` are never written by the atlas — Claude reconciles proposals into them during a session (per CLAUDE.md §2b for BRICKS).
  - **Files:** `.atlas/server.js` (`readBody`, `appendPending`, `POST /api/pending`, async handler, 1 MB body cap), `.atlas/app.js` (`makeEditable`, `postPending`, `showToast`; wired into Vision + Decisions), `.atlas/styles.css` (`.editable` focus ring + `.toast`).
  - **Verified:** `node --check` clean on both. POST a valid proposal → `{ok:true,count:1}`; a second → `count:2`; both persist to `pending.json` with the full shape; missing `text` → **400**; `GET /api/data` surfaces the 2 proposals. DOM-shim render still completes with no throw (61 searchable nodes) after the editable wiring. Cleaned the test `pending.json` (volatile/gitignored).
  - **Notes:** `original` is captured at render time (closure), so the diff is robust even if the user edits twice. `pending.json` is gitignored — proposals are a local queue, not committed state. Field keys (`openQuestion[i]`, `note:<context>`) tell Claude exactly which doc line to update.

- [x] **ATLAS-4 — six-layer UI rendering seeded data (cool editorial palette) (2026-06-22).**
  - **What:** The dashboard itself. `index.html` + `styles.css` + `app.js` render all six layers from `GET /api/data`: **welcome-back ribbon** (N-days-away + resume-here pill from git + the current Doing brick), **Vision** (north-star pull-quote, pitch, 🔵🟡🟠🟢 legend, capability cards with status, open questions), **Progress** (4-column Doing/Next/Blocked/Done kanban), **Decisions** (click-to-flip what→why cards + build-log decision notes), **Learning** (empty-state until ATLAS-6), and **AI Operating Context** grouped by Committed vs Machine-local with ✅/⚠️ verdicts, status labels, promote hints, and the `· last seen <date>` suffix on local-only items in a committed-only snapshot (§F.1). Plus a sticky jump-nav and a live `Filter cards…` search over every card. Palette is deliberately **cool/editorial** (slate-indigo ground, cyan + violet accents, a serif display face for headings) — not a warm-dark theme.
  - **Files:** `.atlas/index.html` (six `<section>` shell + nav), `.atlas/styles.css` (palette + components), `.atlas/app.js` (one render fn per layer + flip + search). All read-only display; edit-in-place is ATLAS-5, review is ATLAS-6.
  - **Verified:** `node --check app.js` clean. **DOM-shim render test** (real data through a minimal fake `document`): boot() completes with no throw; ribbon 4 nodes, vision 5, kanban 4 columns, decisions 2 blocks, learning empty-state, environment 2 groups, sub-caption set, **61 searchable nodes**. Server serves `/`, `/styles.css`, `/app.js`, `/api/data` all **200** with correct content-types; CSS braces balanced (81/81). **Pending:** the visual taste check is the maintainer's (open http://127.0.0.1:7842 on the Mac — "Visual = laptop", per repo convention for UI bricks).
  - **Notes:** No browser in the cloud container, so verification is logic + serving, not pixels. `el()` is a tiny safe DOM builder (text via `createTextNode`, no `innerHTML` except the one escaped ribbon line). Search keys off `data-text` attributes set on every card.

- [x] **ATLAS-3b — `claude-context` adapter + two-tier `environment.json` merge (2026-06-22).**
  - **What:** Built Layer 6's data — the AI operating context + cloud-portability view (plan §F). The `claude-context` adapter emits `environment.json` with two tiers: **committed** (`.claude/` hooks/skills/commands + CLAUDE.md + BRICKS → ✅ `applies-in-cloud`, always present) and **machine-local** (the user's global `~/.claude/CLAUDE.md` memory, `~/.claude/skills/*`, `~/.claude.json` MCP servers → ⚠️ `local-only` + promote hints; **GitNexus** → ⚠️ `local-only-machine-bound`, no promote; Node version → info). The crux is **preserve-don't-delete** (§F.0): machine-local is read only when NOT in cloud *and* `~/.claude` exists (so the cloud's harness `~/.claude` is never mistaken for the user's, per §A); in the cloud the prior committed snapshot's machine-local items are carried forward verbatim, relabelled, never dropped. Items merged by stable `id`, sorted; `markPromoted()` flips a source to `promoted` once its committed artifact exists. Added a `--env-only` flag (refresh just this layer — for the cloud SessionStart hook in ATLAS-9).
  - **Files:** `.atlas/sync.js` (env model + `claude-context` adapter; `ADAPTERS` now 5; `--env-only` in the driver). Writes **committed** `.atlas/data/environment.json`.
  - **Verified (all three §F.4 scenarios):** (1) **cloud run** → `generatedTier: committed-only`, 13 committed items, 0 machine-local (honest — can't see user's `~/.claude`). (2) **simulated user machine** (fake `$HOME`, `CLAUDE_CODE_REMOTE` unset) → `machine-local` tier with `mem:global`/`mcp:context7`/`skill:global:teach` (+promote hints) + `tool:gitnexus` (machineBound) + `info:node`. (3) **cross-environment preservation** — machine-local snapshot as prior, cloud `--env-only` run → all **5 machine-local items preserved + relabelled**, 13 committed intact, `generatedTier→committed-only`, exit 0, **byte-identical** on re-run. `markPromoted` confirmed (mem:global→`promoted` when `.claude/global-memory.md` exists). Restored a clean 13-item committed seed.
  - **Notes:** The committed seed has only committed items today; when the maintainer runs `node .atlas/sync.js` on their Mac, the machine-local tier populates and (once committed) travels into the cloud — that's the whole mechanism. GitNexus detection = `command -v gitnexus` or a `gitnexus` mention in `~/.claude.json`. Promote *artifacts* (`.claude/global-memory.md`, `.mcp.json`) are created by ATLAS-8; `markPromoted` already wired for them.

- [x] **ATLAS-3 — vision + decisions adapters (read-only markdown → vision.json + decisions.json) (2026-06-22).**
  - **What:** Added the two doc-reading adapters (the original ATLAS-3 was split — `claude-context` + the env merge is now ATLAS-3b). **`vision-readme`** parses `vision/README.md` into `vision.json`: north star, one-paragraph pitch, the 🔵🟡🟠🟢 maturity legend, the 6-row capability table (name/line/platforms/status, emoji kept), and the 5 open strategic questions. **`specs-decisions`** parses `docs/superpowers/specs/*.md` `| Decision | Choice | Why |` tables into flip-card `decisions[]`, plus BRICKS.md `**Notes / decisions:**` blocks into secondary `notes[]`. Added shared markdown helpers (`stripInline`, `sectionBody`, `parseTable`, `findDecisionTable`).
  - **Files:** `.atlas/sync.js` (helpers + 2 adapters; `ADAPTERS` now `[git, bricks, vision-readme, specs-decisions]`). Writes committed `.atlas/data/vision.json` + `.atlas/data/decisions.json`.
  - **Verified:** `node .atlas/sync.js` → vision: northStar + 741-char pitch + 4 legend entries + 6 capabilities (SpeakType 🟠 … Agent access 🔵) + 5 questions; decisions: 7 flip-cards from the Mac design spec + 7 BRICKS notes. Both files **idempotent** (`cmp -s` YES on re-run). Confirmed only one spec uses the Decision/Choice/Why convention, so no decisions are missed.
  - **Notes:** Each adapter just appended to `ADAPTERS`; the driver was untouched (the §G seam working as intended). `decisions.notes` capped at 12 to keep the card list scannable. Specs using other decision formats simply contribute nothing — graceful, consistent with §G.2.

- [x] **ATLAS-2 — `sync.js` adapter engine: git + bricks adapters (2026-06-22).**
  - **What:** Built the derivation engine that turns the repo into the atlas's data. It's **adapter-shaped** (plan §G): each source is a `{ id, layer, detect(), read() }` object in an ordered `ADAPTERS` array, and a thin driver runs every adapter whose `detect()` is true, writing the union to `.atlas/data/<layer>.json`. Two adapters ship: **`git`** (`git log -1` + branch + clean-tree → `git.json`, incl. `daysAway` for the welcome-back ribbon) and **`bricks`** (BRICKS.md `- [ ]`/`- [x]` checkboxes → a Done/Doing/Next/Blocked kanban in `progress.json`). Doing = the first actionable open item (the current brick, per the session-start convention); parked/gated items (regex on "deferred/gated/hardware/parked/…") route to Blocked. All dates are day-granular (`YYYY-MM-DD`) so re-runs don't churn.
  - **Files:** `.atlas/sync.js` (new). Writes `.atlas/data/git.json` (volatile, gitignored) + `.atlas/data/progress.json` (committed).
  - **Verified:** `node .atlas/sync.js` → both files generated; kanban correct (doing = `SCHEMA-CONSISTENCY`, 18 next, 5 blocked incl. SHELF-DROP-EDGES/SHELF-LATER, 4 done). **Idempotent:** re-run → `git.json` + `progress.json` byte-identical (`cmp -s` YES). **Graceful degradation:** `mv BRICKS.md` away → `bricks` skipped, `git` still writes, exit 0, no crash — the exact behavior a future drop-into-any-repo run relies on (§G.2).
  - **Notes:** New adapters (vision, decisions, claude-context in ATLAS-3) just append to `ADAPTERS` — the driver is untouched; that's the seam working. `cleanTree:false` is expected right now (the atlas work itself is uncommitted). Idempotency holds within a day; crossing midnight changes `daysAway`/`generatedAt` by design.

- [x] **ATLAS-1 — `.atlas/` scaffold + minimal server + STRUCTURE.md row + .gitignore (2026-06-22).**
  - **What:** Stood up the zero-dependency skeleton so `node .atlas/server.js` serves a dashboard shell and exposes the (not-yet-generated) derived data. The server statically serves `.atlas/` files and answers `GET /api/data` with the union of the seven data layers (all `null` until `sync.js` runs). Fixed localhost port **7842**.
  - **Files:** `.atlas/server.js` (http server, static + `/api/data`, path-traversal guard), `.atlas/index.html` + `.atlas/styles.css` + `.atlas/app.js` (boot stub that fetches `/api/data`), `.atlas/README.md` (run instructions, committed-vs-volatile data policy), `STRUCTURE.md` (new `.atlas/` row in the repository map, per §7), `.gitignore` (volatile `git.json`/`pending.json` + `graphify-out/`; everything else under `.atlas/` is committed so it travels).
  - **Verified:** `node .atlas/server.js` → `GET /` returns `index.html`; `GET /api/data` → `{git:null,…,pending:null}`; `styles.css` served as `text/css`; encoded path traversal (`/%2e%2e/CLAUDE.md`) → **403**; literal `..` collapses client-side → 404. Server binds `127.0.0.1:7842` only.
  - **Notes:** Port 7842 documented in `.atlas/README.md`. `environment.json` is intentionally NOT gitignored (it carries the machine-local-config view into the cloud — §F.0). Boot UI is a deliberate stub; the full six-layer render is ATLAS-4. No `git commit` yet — building in the working tree on `feat/audiobook-agent`; the maintainer commits when ready.

## Mac (Swift) — on `main` (trunk)

_Native Swift macOS dictation app (a deliberate **fork** — Windows stays C#, they evolve independently). Spec: `docs/superpowers/specs/2026-06-04-speaktype-mac-design.md`. Lives in `mac/` (SwiftPM). Decisions: SwiftUI `MenuBarExtra`, WhisperKit (Neural Engine), hold-Fn (🌐) push-to-talk, SwiftPM + `build-app.sh`, local-dev ad-hoc-signed arm64. Ports-and-adapters with a pure tested `DictationCoordinator`. Domain `Skill:` lines are `none` (the `dotnet-*` skills don't apply to Swift, per CLAUDE.md §6); TDD + verification still apply._

### Next up (Mac)

_UI redesign — floating pill + scratchpad. Spec: `docs/superpowers/specs/2026-06-04-speaktype-mac-ui-redesign-design.md`. Pure/testable core first, then adapters, then UI, then polish. Domain `Skill:` lines are `none` (no Swift skill, per §6); process skills (TDD, verification) still apply. UI/animation bricks verify manually on the Mac._

_**UI redesign complete** — UI-1…UI-11 all shipped (floating pill + scratchpad). All remaining items are pending the user's manual visual pass on the signed `.app` (see each Done entry's "Manual (Mac)" note)._

_**Co-edit polish** (`feat/mac-app`) — round complete and user-re-tested: **TRAIL** (+clear-stale ✓), **HOLD-180**, **TESTDOCS**, **POLISH-UI**, **PASTE-HONEST** (all green; HOLD-180/TESTDOCS archived). Mac development continues on this branch toward launch (full `TESTING.md` major pass → notarize + DMG); `main` was held untouched until launch, then **merged up to `feat/mac-app` on 2026-06-20 when the repo went public (MIT)** — `main` now mirrors this branch._

_**Repo tidy (2026-06-07):** the old root .NET solution was parked under **`windows/`** on this branch with a **`windows/STATUS.md`** note — it's a pre-split baseline; the live Windows work is on branches `chore/windows-folder` + `feat/correction-pipeline`, to be resumed after the Mac launch (replicate Mac UX to parity + merge correction-pipeline). See the Windows section below._

_**windows/ phantom deletions — investigated & guarded (2026-06-10):** `windows/SpeakType.App` was repeatedly deleted from the **worktree** (never committed) during the user's manual Shelf drag-test rounds. Ruled out by direct testing: GitNexus hooks (`augment`/`analyze` leave the tree clean), `build-app.sh` from any cwd, the app's payload-delete path (App-Support-scoped), all project hooks (read-only), gitignore case-collisions, and parallel Claude sessions (transcripts show one lineage). Conclusion: a **Finder-side move during a drag gesture** (the panel is a small target that can slip away mid-drag; Finder spring-loading makes a fumbled release a silent move — and a landing inside the gitignored `SpeakTypeMac/SpeakType.app` bundle, case-insensitively the same name, is invisible to git and erased by the next build's `rm -rf`). **Guards now in place:** (1) `.claude/hooks/restore-windows-baseline.sh` (SessionStart + Stop) auto-restores any *unstaged* deletion under the baseline — verified against simulated damage; intentional staged `git rm` is respected. (2) Drag tests use `~/SpeakType-TestFiles/` (disposable samples), never repo files — noted in TESTING.md._

#### Next iterations — post-launch direction (queued 2026-06-12, user-dictated)

_The user's four next bets, in their words distilled. None is specced yet — each starts with a brainstorm → spec → plan cycle (CLAUDE.md flow) before any brick is cut._

- [ ] **SKILLS-FOR-SMALL-MODELS — lab eval: steering small local models with skills.** Research brick, lab-side: test how well Claude-style *skill files* (on-demand task instructions) control the behavior of small on-device models vs. baking instructions into the system prompt — format adherence, behavior control, cross-model regression (Gemma/Qwen/Gemma-4 from the existing bake-off). Output: a human-readable lab report (gentle thermal-testing rules apply) that decides the instruction architecture for Sidekit Intelligence.
  - Skill: none (lab eval design first)

#### Shelf + Mirror — Sidekit launch features (queued 2026-06-09)

_The two missing launch features for the Sidekit pivot (vision: launch = SpeakType + Shelf + Mirror). Decision-resolved specs: `docs/superpowers/specs/2026-06-09-sidekit-shelf-design.md` and `...-sidekit-mirror-design.md` (both grilled with the user). Pure/testable core first, then adapters, then UI, then the drag-start spike, then polish. Mirror rides on the Shelf panel, so `SHELF-PANEL` must land before the Mirror bricks. Domain `Skill:` lines are `none` (no Swift skill, per §6); process skills (TDD, verification) apply; UI/drag/camera bricks verify manually on the Mac._

_**SHELF-DROP split (2026-06-10):** the original SHELF-DROP brick was over the §2a hard ceiling, so it was split. **SHELF-PAYLOAD** (the `ShelfPayloadStore` port + `FileSystemShelfPayloadStore` deletion + ShelfStore wiring) is **done** (see Done); the remaining four are below._

- [ ] **SHELF-DROP-EDGES** *(remainder is sandbox-gated)* — **(a) done 2026-06-10:** the vector/PDF-backed image drop (`pngData == nil`) now logs `image drop has no encodable bitmap representation — skipped` instead of vanishing silently (shipped with SHELF-POLISH). **(b) remains:** **security-scoped file URLs** — when the app is eventually **sandboxed** (Mac App Store / Sidekit), `copyItem` on a dropped file needs `startAccessingSecurityScopedResource()` / `stopAccessing…` or every file drop fails. Latent today (no App Sandbox entitlement) — do when sandboxing lands.
  - Skill: none (verification-before-completion)
- [ ] **SHELF-LATER** *(post-v1, parked with reasons — from SHELF-POLISH deferrals)* — cancel-mid-copy for huge folders (needs a custom cancellable recursive copier; `FileManager.copyItem` can't abort), tile-direct multi-drag (unify tap+drag in one AppKit layer per tile — only after hover-reveal is verified to survive an overlay), auto-summon on non-file drags (spec scopes the summon to *file* drags).
  - Skill: none
- [ ] **MIRROR-UAT-DEFERRED** *(2 hardware-blocked checks from the 2026-06-11 Mirror UAT)* — re-verify when the hardware is available: **(a) source picker** switches feeds with **>1 camera** attached (user had only the built-in cam), **(b) "No camera found"** notice shows with **no camera** attached (couldn't disconnect the built-in). Code paths exist (MIRROR-UI/CAM/POLISH); only the live confirmation is pending. Everything else in the Mirror UAT passed (24/26).
  - Skill: none (verification-before-completion)
_**Mirror v1 — COMPLETE** (2026-06-11): all five v1 bricks shipped (MIRROR-CORE · CAM · UI · LIFECYCLE · POLISH). Live front-camera self-view on the Shelf panel, build-verified locally; the live-camera behavior (green light, mirroring, source switching) awaits the user's manual pass on the signed `.app` — see each Mirror Done/archive entry's "Manual (Mac)" note._

_**Mirror additions — full-screen + edge light — COMPLETE (2026-06-11).** All three bricks shipped (MIRROR-FS-CORE · FS-UI · EDGELIGHT). Decision-resolved spec: `docs/superpowers/specs/2026-06-11-sidekit-mirror-fullscreen-edgelight-design.md`. Full-display **overlay** (borderless, non-activating, ✕/click/Esc to exit, returns to `.expanded`) + a **manual** edge light (☀ toggle, **no frame sampling** — privacy-clean) drawing a **solid bright-white inset frame** (thin windowed, thick overlay). Build-verified locally; the live-camera/overlay/edge-light behavior awaits the user's manual pass on the signed `.app` — see each Done/archive entry's "Manual (Mac)" note._

_**🚀 LAUNCHED (2026-06-12):** Mac **v0.1.0** is public. Site live at **https://sidekit.app** (Vercel project `sidekit`; source `~/SideKit Website/site`, its own git repo `sidekit-site` — NOT this repo). DMG hosted on the public **`omkareshwar-tripathi/sidekit-releases`** repo; the site links `releases/latest/download/Sidekit.dmg`, so future releases just upload a new asset with that exact name (no site redeploy). M9 UAT: 19 machine + 5 human checks passed, 0 fails. Source repo **went public (MIT) on 2026-06-20**; `main` was brought up to date with `feat/mac-app`, so the public default branch now mirrors the launch branch (see `STRUCTURE.md`)._

- [ ] **NOTARIZE — Apple Developer ID + notarized DMG (user enrolling morning of 2026-06-12).** The shipped build is ad-hoc-signed: downloaders hit Gatekeeper ("Apple could not verify…") and on macOS 15+ must use System Settings → Privacy & Security → **Open Anyway** (site + release notes now carry the correct steps; confirmed live on the user's own machine, dialog offers only Done/Move to Bin). **Morning flow:** (1) user enrolls at developer.apple.com/programs/enroll ($99/yr; individual approval usually <1 day); (2) user creates an **App Store Connect API key** (appstoreconnect.apple.com → Users & Access → Integrations → App Store Connect API → Team key, role Developer/Admin) and hands over the **.p8 file + Key ID + Issuer ID**; (3) Claude does the rest: create the Developer ID Application cert (via ASC API w/ a CSR), switch `mac/Scripts/build-app.sh` to Developer ID signing + hardened runtime + (likely) entitlements check, `xcrun notarytool submit --wait` the DMG, `xcrun stapler staple`, rebuild, upload as `Sidekit.dmg` to a new `sidekit-releases` release (site needs no change — it links `latest/`), then verify like the UAT: download fresh, confirm Gatekeeper opens it clean. MAS is NOT an alternative (sandbox forbids the Fn monitor + paste Accessibility powers).
  - Skill: none (verification-before-completion)

#### Sign-up + feedback — manual pass (queued 2026-06-12)

- [ ] **SHIP-M9 — REMAINDER: dictation + eyeball checks (user, ~3 min).** Most of M9 was **machine-walked 2026-06-12** by driving the real UI via Accessibility scripting + screenshots (results in the SHIP Done entry): welcome panel renders & gates correctly (invalid email blocked, valid signs up → dashboard row), 6-launch lifecycle counters exact (show #1 → quiet 2–4 → one re-ask #5 → never), feedback box (focus-on-open, ⌘↩ send → "Thanks!" toast → row, Esc keeps draft, 🐞 chip → `type=bug`), offline → "Saved…" toast → `outbox.json` → relaunch flush → row. Test rows cleaned; the user's real email restored locally and is the only `signups` row. **Still needs a human (no voice/Fn here):** `mac/UAT-Signup-Feedback.html` section "Dictate your feedback" — hold Fn, speak into the feedback box, ⌘↩, row lands; then the M5 routing regression (note + paste-at-cursor); plus a quick visual-polish glance at the welcome panel & feedback box (machine saw them, taste check is human) and the optional launch-at-login w6. Dashboard: <https://supabase.com/dashboard/project/ubqdjvbhanhnxlonqrii>.
  - Skill: none (verification-before-completion)

### Done (Mac)

- [x] **HOMEBREW-DIST — Homebrew tap + install docs for the un-notarized DMG (2026-06-22).** User couldn't distribute the DMG — on **macOS 26 (Tahoe)** Gatekeeper hard-rejects the self-signed (`Sidekit Local Signing`), un-notarized app once it carries the download *quarantine* flag (the block dialog offers only Done / Move to Bin → looks like "won't open / does nothing"). **Root-caused by reproduction on the user's Mac:** signature valid on disk, `codesign --verify` OK, but `spctl -a` **rejected**; the app launches fine the instant quarantine is removed. Distribution channel is irrelevant — DMG / zip / GitHub Release all get quarantined identically; the only real fix is **notarization** (still the open `NOTARIZE` brick). User chose the **free workaround + a Homebrew path**. **What ships:** **(1) new PUBLIC repo `omkareshwar-tripathi/homebrew-sidekit`** — the **4th** Sidekit repo; Homebrew *requires* a standalone `homebrew-`-named tap, it cannot live inside another repo. Holds `Casks/sidekit.rb` (v0.1.0, sha256 `9400f451…f03590`, url → `sidekit-releases/releases/download/v0.1.0/Sidekit.dmg` with `verified:`, `app "Sidekit.app"`, `depends_on macos: :sonoma` + `arch: :arm64`) + a README. **(2) install docs** — `Sidekit` repo `README.md` gains an "Install (no build needed)" section (Homebrew recommended + direct-download with one-time **Open Anyway** / `xattr -dr com.apple.quarantine /Applications/Sidekit.app` steps); `sidekit-site` `index.html` download card links the tap + clearer first-launch steps. **Homebrew 6.0 gotchas a future session MUST know:** (a) `--no-quarantine` was **removed** in brew 6.0 (no flag, no env) → the cask clears quarantine itself in a **`postflight`** (`xattr -dr com.apple.quarantine "#{appdir}/Sidekit.app"`) so `brew install` opens with **no Gatekeeper warning**; (b) brew 6.0 **requires `brew trust`** for any third-party cask (`HOMEBREW_REQUIRE_TAP_TRUST` is default-ON, shown by `brew config` even when unset in env) — trust is **content-pinned**, so it must be re-run after each cask edit (one-time for real users). **Install flow:** `brew tap omkareshwar-tripathi/sidekit` → `brew trust --cask omkareshwar-tripathi/sidekit/sidekit` → `brew install --cask sidekit`. **Files:** NEW repo `homebrew-sidekit` (`Casks/sidekit.rb`, `README.md`); `Sidekit/README.md`; `sidekit-site/index.html`. **Verified:** full install on the user's Mac end-to-end — installed to `/Applications`, postflight cleared the quarantine attr, app launched (`open` exit 0); `brew audit --cask --online sidekit` clean (after fixing a deprecated `depends_on macos:` string form + adding `verified:`); README pushed to `main` (`cb49928`); site deployed — `curl https://sidekit.app` → 200 with the new "Install from the tap" / "Open Anyway" strings present. **Notes / follow-ups:** the real fix is still **NOTARIZE** (the free path needs one manual trust action per downloader); **on every new release**, bump `version` + `sha256` in the cask (users then `brew upgrade --cask sidekit`). **4 repos total now** — `Sidekit` (code), `sidekit-releases` (DMG), `sidekit-site` (private, site), `homebrew-sidekit` (tap). `sidekit-releases` *could* fold into the main repo's Releases now that source is public, but the user decided to **keep the 4-repo split** (re-pointing the cask URL + site link + re-testing isn't worth it); revisit only at a release-pipeline overhaul.

- [x] **INTEL-UI-FIXES — hover trigger, close button, growing panel (2026-06-12, user M11 feedback round 1).** Three small bricks from the user's first walk of Intelligence v1, all shipped same-day. **(1) INTEL-UI-HOVER** `860ec42` — the invisible hover pad shrank 260x36 -> **80x30** (spec section-2 floor): the dot alone now triggers the bloom; while EXPANDED the menu's own glass card hit-tests and keeps the hover region alive, so the big always-on pad was never needed. **(2) INTEL-UI-CLOSE** `c1558b6` — visible **x close button** in the scratchpad header (ShelfView house style, `.help("Close")`); Esc still works. **(3) INTEL-UI-GROW** `5cabdcb` — root cause of "can't see everything / no Copy button": the panel window was a FIXED 480x420 and a result card pushed the Copy row below the clip. Now: tall transparent canvas (560x760), card bottom-anchored (grows UPWARD above the pill; unused area is fully transparent + click-through per-pixel), width 460 -> 500, editor 110 -> 130, result block is `ViewThatFits` (sizes to the text, caps at 300 pt then scrolls) so the Copy/Use-as-input row is always visible. **Verified:** `swift build` clean per brick; `swift test` 184/184; `.app` rebuilt for the user's re-test. **Pending:** user re-test of all three on the rebuilt .app (plus the rest of M11).

- [x] **INTELLIGENCE-V1 — pill hover menu, scratchpad panel, RAM-guest session, Settings Intelligence row (2026-06-12).** Final task of the Sidekit Intelligence v1 plan. **What ships:** (1) **Pill hover menu** — 3-button menu (Polish / Scratchpad / Dictate) blooms on hover over the idle dot; 260×36 tracking region (widened from 120×32 to cover the bloomed menu footprint per review fix); mouse-away collapses. (2) **Scratchpad panel** — floating glass panel with a TextEditor, Polish + Draft chips, segmented tone picker (persisted to UserDefaults), Copy/Use-as-input result row, honest status line (needsModel → download button, downloading → progress %, loading → "Warming up…", generating → "Drafting…"), Esc-to-close with draft surviving, dictation routed in while the panel is key. (3) **RAM-guest IntelligenceSession** — loads the model on first chip use, generates, idles for 180 s then unloads, responds to `MemoryPressureSource` (DispatchSource `.warning`+`.critical`) to evict immediately. ONE on-device model **Qwen2.5-1.5B-Instruct-4bit** (~0.85 GB disk, resolves to ~1.1 GB RAM) serves both polish and draft; the session detects the shared engine and relabels role switches without reloading. One-time in-app download via `ModelDownloader`. (4) **Settings Intelligence row** — `SettingsView` gains `intelligence: IntelligencePanelModel?`; new `IntelligenceSettingsRow` shows "downloaded" + destructive **Remove model** button (calls `removeModel()`) or "not downloaded" caption; wired through `MainWindow` → `App.swift` → `controller.intelligence`. (5) **mlx.metallib** — compiled Metal shaders staged in `AppBundle/mlx.metallib` and bundled by `build-app.sh` (ship blocker fixed). **Model saga (decisions a future session must know):** Gate A killed one-shared-Qwen3.5 (127/190) → two-model split (Gemma polish 148/190 + Qwen3 draft) → live selftest exposed an **upstream mlx-swift-lm 3.31.3 Gemma-2 forward-pass defect** (greedy derailment, prefill-size-dependent; Python-correct on identical snapshot/prompt; Qwen3 Swift≈Python so the stack is fine) → Qwen3-1.7B scored 75/190 polish (disqualified) → user decision: **ship single-model Qwen2.5-1.5B** (polish 134/190, assistant 23/29, guardrail 2/2). Greedy temp 0.0 everywhere (matches all lab numbers). Spec/plan amended (round 3); all numbers recorded in spec §1/§7. **Review fixes applied:** hover pad widened 120×32 → 260×36 to cover bloomed menu footprint (tracking-region stability); two-model wording retired from role docs. **Files:** `mac/Sources/SidekitApp/PillView.swift`, `mac/Sources/SidekitApp/IntelligencePanel.swift`, `mac/Sources/SidekitCore/IntelligenceSession.swift`, `mac/Sources/SidekitApp/SettingsPanel.swift`, `mac/Sources/SidekitApp/MainWindow.swift`, `mac/Sources/SidekitApp/App.swift`, `mac/AppBundle/mlx.metallib`, `mac/Scripts/build-app.sh`, `mac/TESTING.md` (§11 + count updated to 184). **Verified:** `swift build` clean; `swift test` **184/184**; `IntelligenceSelftest` PASS (load 0.35 s, polish gen 1.72 s, draft gen 0.91 s, disk 0.82 GB; polish output byte-identical to lab's sc-8 case); `.app` assembles with metallib and launches. **M11 human pass: PENDING** — TESTING.md §11 written; user was away during this session. **Follow-ups:** run M11 (human); file the upstream mlx-swift-lm Gemma-2 bug; prompt fine-tuning for Qwen2.5 polish in the lab (user wants to "fine tune later"); streaming token output if generation feels slow; auto-polish toggle still future; clipboard-screenshot product question still open.

## Windows (C#) — PARKED (prototype removed 2026-06-20)

> **The Windows .NET prototype and its CI were removed on 2026-06-20** (git history has the code). Windows stays a **planned** platform in [`vision/`](vision/ROADMAP.md) — it will be rebuilt later. Everything below is the **historical record of that removed prototype**, kept as reference for the future rebuild; it is **not active work**.

## Next up (historical — the .NET prototype was removed 2026-06-20)

_(Was the Windows build queue. Kept for the future rebuild; not active.)_

### App shell, UI & polish

_Modern-light UI restyle (Option 1, light-only). Spec: `docs/superpowers/specs/2026-06-03-ui-modern-light-restyle-design.md`; plan: `docs/superpowers/plans/2026-06-03-ui-modern-light-restyle.md`. All Windows-only → verify = CI compile-green + a laptop screenshot vs mockup B (no Core changes; 179 tests stay green)._

### Integration & ship

### Backlog (optional — needs a user decision, not in the v1 critical path)

- [ ] **Real installer (Start Menu + Desktop shortcut + uninstall) — deferred by user (2026-06-03).** Today Sidekit is a portable single exe: install = download `Sidekit.exe` from a CI artifact (Brick 18) and run it; first run adds a "Start with Windows" Run-key entry and lives in the tray, but creates **no** Start Menu/Desktop shortcut and has no uninstaller. User said they'll "work on installer later." Options when picked up: (a) lightweight — have the app create/remove a Start Menu + Desktop shortcut on first run / cleanup (small Windows-only brick, `IWshShortcut`/COM or a `.lnk` writer); (b) proper — an **MSI / Inno Setup / WiX** installer that handles shortcuts + uninstall + (eventually) code signing to kill the SmartScreen warning. Pairs naturally with a versioned GitHub **Release** (vs the current 90-day CI artifact). Windows-only.
  - Skill: dotnet-best-practices, run-tests


- [ ] **Clipboard contention + paste error handling (revealed by Brick 8 review).** WinForms `Clipboard` throws transient `ExternalException` when another process holds the clipboard open; today that propagates out of `ClipboardPasteService.Paste` (and on Windows, up through the hotkey hook callback). Add best-effort retry/swallow in the `WinClipboard` adapter and decide the user-facing failure surface. Tightly coupled to Brick 9 (global exception handling) and Brick 14 (threading / marshalling the paste onto the STA thread) — fold in there rather than as a standalone brick if convenient.
  - Skill: dotnet-best-practices, run-tests
- [ ] **Consolidate the duplicate capturing `ILogSink` test double (revealed by Brick 14d review).** `FakeLogSink` (`Sidekit.Tests/Orchestration/Fakes.cs`, added in 14d) is byte-for-byte the private `FakeSink` nested in `Sidekit.Tests/Logging/AppLoggerTests.cs` (Brick 13). Make `AppLoggerTests` use the shared `FakeLogSink` and delete its private copy. Deferred from 14d because it edits a shipped test file outside that brick's scope; rule-of-three only just met. Test-only, Mac-testable.
  - Skill: dotnet-xunit, run-tests
- [ ] **Shared test temp-dir helper (revealed by Brick 6b review).** The temp-dir scaffolding (`_tempDir` field + ctor + `Dispose`) is now duplicated across `JsonSettingsStoreTests`, `ModelStoreTests`, and `HttpModelDownloaderTests` (rule-of-three met). Extract a tiny `TempDir`/`TempDirFixture` IDisposable helper and have the three classes use it (~12 lines saved each). Deferred from Brick 6b to keep that brick from editing already-shipped test files; do it as its own small test-only brick.
  - Skill: dotnet-xunit, run-tests
- [ ] **Deferred-dispatcher SUT-builder test helper (revealed by Brick 14e review).** Four orchestrator tests now hand-build an orchestrator + `DeferredDispatcher` + outcomes list with the same ~11-line block (the in-flight-cycle cases that the shared `_sut` — built without a dispatcher — can't exercise). Extract a `BuildWithDeferredDispatcher(...)` helper returning the SUT + the fakes the tests assert on. Deferred from 14e because the fix would edit the three older tests outside that brick's scope. Test-only, Mac-testable.
  - Skill: dotnet-xunit, run-tests
- [ ] **Reconcile `AppSettings.Autostart` with an externally-edited Run key (revealed by Brick 14e review).** The `ApplyAutostart` funnel keeps the two in-app entry points in sync, but if the user removes Sidekit from startup *externally* (Task Manager → Startup, msconfig) between sessions, the next launch reconciles only the tray checkmark (`tray.SetStartWithWindowsChecked(autostart.IsEnabled())`); `settings.Autostart` keeps the stale `true` on disk and in the Settings checkbox — a three-way divergence. Pre-existing (the funnel didn't introduce it). Decide the source of truth (likely the Run key) and sync `settings.Autostart`/save at startup when the first-run gate is skipped. Windows-only; verify with M6.
  - Skill: dotnet-best-practices, run-tests

- [ ] **Cleanup-pipeline real-world hardening (revealed by Brick 2 review).** Decide whether to: (a) trim the always-filler list so it stops eating real tokens — `mm` (millimeter), `er` (ER), and possibly `ah` collide with genuine words/units; (b) make `i`→`I` skip abbreviation/list contexts like `i.e.` / `Section i` / `for i`; (c) fix the two niche defects — stacked leading fillers losing capitalization (`"Um er,"`) and hyphen-joined clusters (`"Mm-hmm."`). All three currently behave per spec lines 121/129; changing them is a spec decision, hence parked here rather than done autonomously.
  - Skill: dotnet-best-practices, dotnet-xunit, run-tests

## Done

_(Newest first. Older entries archived to `BRICKS-ARCHIVE.md`.)_

### Brick 18 — CI publishes a downloadable SpeakType.exe (2026-06-03)
- **What:** Made the app installable without a build. CI already published the self-contained single-file exe on every run but threw it away; added an `actions/upload-artifact@v4` step so each run attaches **`SpeakType-win-x64` → SpeakType.exe** to its "Artifacts" section. Installing is now: download the exe from a green run and double-click it — no .NET SDK, no Git, no local build. Updated the README with this download path.
- **Files:** `.github/workflows/ci.yml` (upload-artifact step); `README.md` (new "Download (no build)" section).
- **Verified:** Windows-only (CI) → **Windows x64 CI green** (run 26846751774) and the artifact is confirmed attached via the GitHub API: `SpeakType-win-x64`, **69,848,648 bytes (~66.6 MB)**, 90-day retention. Core suite untouched → **179/179**. Reviewed: YAML indentation matches sibling steps, `@v4` is current, `with:` schema valid (`name`/`path`/`if-no-files-found: error`), `path` matches the publish/verify exe path, step runs after publish so the file exists, and artifact upload is fine on PR runs too.
- **Notes / decisions:**
  - **Artifact, not a tagged Release:** every green run yields a downloadable exe (simplest, always-on) — no tagging step. A versioned GitHub **Release** (attach the exe on a `v*` tag) is the natural next step if/when we want stable, non-expiring download links.
  - **`if-no-files-found: error`** turns a silently-missing exe into a red run (defence-in-depth alongside the existing verify step, which still logs the size).
  - The exe is **unsigned** → SmartScreen "More info → Run anyway" still applies (code signing remains out of scope for v1).

### Brick UI-6 — App logo (waveform mark) (2026-06-03)
- **What:** Gave SpeakType a real logo (user request; concept **C** — a white equalizer/waveform on an accent-blue rounded tile). New `AppIcon` brand helper + a multi-size `Assets/speaktype.ico` (16–256px). The icon now appears as: the **.exe icon** (`<ApplicationIcon>`), the **Settings & Welcome window title bars / taskbar / Alt-Tab** (`Icon = AppIcon.Brand`, loaded from the embedded ico), and the **tray icon** — which keeps its per-state colour signal by drawing the *same waveform glyph* tinted blue/red/orange/dark-red (`AppIcon.ForState`) instead of the old plain circles.
- **Files:** `SpeakType.App/Assets/speaktype.ico` (new asset); `SpeakType.App/Branding/AppIcon.cs` (new — `Brand` + `ForState`, plus the GetHicon/DestroyIcon idiom moved here from TrayIcon); `SpeakType.App/SpeakType.App.csproj` (`ApplicationIcon` + embedded resource w/ `LogicalName`); `SpeakType.App/Tray/TrayIcon.cs` (use `ForState`, removed `MakeIcon`/`DestroyIcon`/`partial`); `SpeakType.App/Settings/SettingsForm.cs` + `SpeakType.App/Startup/WelcomeForm.cs` (`Icon = AppIcon.Brand`). No Core/test changes.
- **Verified:** Windows-only → **Windows x64 CI green** (run 26846172886) — incl. the publish step, which proves `<ApplicationIcon>` resolved and the exe built with the icon. Core suite untouched → **179/179**. `/code-review` → **all 8 risks REFUTED** (key one: WinForms `Form` does NOT dispose an assigned `Icon` — only its own derived small icon — so the shared process-lifetime `Brand` is safe across the repeatedly-created Welcome window; `LogicalName` makes `GetManifestResourceStream("speaktype.ico")` resolve; `partial`/using removals compile clean; HICON cleanup leak-free). `/simplify` → applied 2 nits (reworded an overstated comment; `FillRoundedBar` de-extension-methodised). **Visual = laptop: check the exe icon in Explorer, the window title-bar/taskbar icon, and the tray glyph colour per state — screenshot if anything's off.**
- **Notes / decisions:**
  - **The `.ico` is the single brand source** (exe + windows load it); only the **tray** is drawn programmatically, because it needs runtime per-state tinting (a static asset can't recolour). The tray glyph's bar proportions are hand-matched to the .ico.
  - **`AppIcon.Brand` is shared & never disposed** (process-lifetime, like the `UiTheme` fonts) — safe because Form doesn't own/dispose an assigned Icon.
  - **Tooling:** the `.ico` was generated on the Mac with Python/Pillow (no ImageMagick needed); regenerate via the same waveform proportions if the mark is revised.

### Brick UI-5 — Flat tray menu (2026-06-03)
- **What:** Final brick of the modern-light restyle. The tray right-click `ContextMenuStrip` now uses a `ToolStripProfessionalRenderer` backed by `FlatMenuColorTable` plus light font/colours (`UiTheme.Body`/`TextPrimary`/`Surface`, `RoundedEdges = false`) — a flat white menu with accent hover and a thin grey separator, replacing the legacy gray-gradient chrome. Menu items, the separator, checkmarks (Pause / Start with Windows) and **all Click wiring are unchanged**.
- **Files:** `SpeakType.App/Tray/TrayIcon.cs` (using + 4 lines in `BuildMenu`). No Core/test changes.
- **Verified:** Windows-only → **Windows x64 CI green** (run 26845342026, build + publish) = compile-green. Core suite untouched → **179/179**. Combined `/code-review` + `/simplify` → **clean**: `RoundedEdges` is a valid property; subclassing only the colour table leaves checkmark rendering intact; the renderer + `FlatMenuColorTable` aren't `IDisposable` and hold no unmanaged handles → no leak, nothing to dispose; behaviour-preserving. **Visual + menu actions = laptop screenshot (right-click the tray icon).**
- **Notes / decisions:**
  - **Tray *icon* glyphs unchanged** — the four state circles (SteelBlue/Red/Orange/DarkRed via `MakeIcon`) still convey Idle/Recording/Busy/Error; only the *menu* chrome changed. (A logo for the icon itself is the separate Brick UI-6.)

<!-- Template for each entry:

### Brick N — <title> (YYYY-MM-DD)
- **What:** one line on what it does / what the user sees.
- **Files:** main source files touched (+ tests).
- **Verified:** how you confirmed it works (tests + manual M# items).
- **Notes:** decisions, gotchas, or follow-ups a future session needs.

-->
